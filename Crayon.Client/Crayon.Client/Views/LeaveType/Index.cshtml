@model Crayon.Entity.Common.Result<List<Crayon.Entity.Dto.LeaveTypeResponse>>

@{
    ViewData["Title"] = "Leave Types";
}

<div class="page-header">
    <h2>Leave Types</h2>
    <a asp-action="Create" class="btn btn-primary">Add Leave Type</a>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table table-hover datatable w-100">
            <thead>
                <tr>
                    <th>Leave Name</th>
                    <th>Default Days</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model?.Response != null)
                {
                    foreach (var item in Model.Response)
                    {
                        <tr>
                            <td>@item.LeaveName</td>
                            <td>@item.DefaultDays</td>
                            <td>
                                <a asp-action="Details"
                                   asp-route-id="@item.LeaveTypeId"
                                   class="btn btn-info btn-sm">
                                    View
                                </a>
                                <a asp-action="Edit"
                                   asp-route-id="@item.LeaveTypeId"
                                   class="btn btn-warning btn-sm">
                                    Edit
                                </a>
                                <a asp-action="Delete"
                                   asp-route-id="@item.LeaveTypeId"
                                   class="btn btn-danger btn-sm confirm-delete"
                                   data-name="@item.LeaveName" ">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
