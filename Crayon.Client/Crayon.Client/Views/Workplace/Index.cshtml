@using Crayon.Entity.Common
@using Crayon.Entity.Dto
@model Result<List<WorkplaceResponse>>

@{
    ViewData["Title"] = "Workplaces";
}

<div class="page-header">
    <h2>Workplaces</h2>
    <a asp-action="Create" class="btn btn-primary">Add Workplace</a>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table table-hover datatable w-100">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model?.Response != null)
                {
                    foreach (var item in Model.Response)
                    {
                        <tr>
                            <td>@item.WorkplaceId</td>
                            <td>@item.Name</td>
                            <td>@item.Address</td>
                            <td>
                                <a asp-action="Details"
                                   asp-route-id="@item.WorkplaceId"
                                   class="btn btn-info btn-sm">
                                    View
                                </a>
                                <a asp-action="Edit"
                                   asp-route-id="@item.WorkplaceId"
                                   class="btn btn-warning btn-sm">
                                    Edit
                                </a>
                                <a asp-action="Delete"
                                   asp-route-id="@item.WorkplaceId"
                                   class="btn btn-danger btn-sm confirm-delete"
                                   data-name="@item.Name">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
